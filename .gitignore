# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test / tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Training artefacts — regenerate by re-running; too large for git
output/
# Downloaded run artifacts. The trailing-slash form missed the timestamped
# variants the pull scripts actually create (kaggle_results_stale_1786419155/
# and friends) — 117MB of them were sitting untracked, one `git add -A`
# away from the history.
kaggle_results*/
artifacts/
*.safetensors

# Local env
.env
.DS_Store

# Literature review PDF — likely contains copyrighted paper content.
# Excluded from the public repo deliberately.
RL LIT REVIEW.pdf
